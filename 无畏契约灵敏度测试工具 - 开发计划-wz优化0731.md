# 无畏契约灵敏度测试工具 - 开发计划

## 项目概述
**目标**：通过科学测试方法，帮助玩家找到最适合自己的鼠标灵敏度（20-80 cm/360°范围）

**核心算法**：贝叶斯优化 + 多维性能评估

**开发引擎**：Godot 4.7.1


## Phase 1: 主菜单与导航

### 1.1 主菜单场景 (Main Menu)
- [x] 创建主菜单UI布局
- [x] 创建场景
  - [x] 主场景
  - [x] 测试选择场景
  - [x] 历史记录场景
  - [x] 设置场景
  - [x] 关于场景
- [x] 在 main.scene 创建 hud，实现按钮交互，链接场景。在每个场景内，暂时添加一个返回主场景的按钮。
  - [x] "灵敏度测试" → 测试选择场景
  - [x] "历史记录" → 历史记录场景
  - [x] "设置" → 设置场景
  - [x] "关于/用户指南" → 关于场景
  - [x] "退出" → 退出应用
- [x] 整体配色调整为无畏契约风格。

### 1.2 设置场景
- [x] 创建设置 HUD
- [x] 设置默认鼠标 DPI
- [x] 设置 FOV 103 70.5
- [x] 音量控制
- [x] 保存设置到本地配置文件，以便加载复用。

---

## Phase 2: 测试选择与准备

### 2.0 场景创建
- 创建场景
  - [x] 创建4个占位场景，test_select test_config test_shoot test_result，按顺序，每个场景内添加指向下一个场景的按钮，以及回退上一个场景的按钮。


### 2.1 测试选择场景
- [x] 创建测试选择UI
- [x] 实现"二分法(PSA)"测试入口
- [x] 实现"Consistency测试"入口
- [x] 添加测试说明弹窗
- [x] 实现测试参数配置面板
- [x] 添加"开始测试"按钮 → 进入测试场景

### 2.2 测试参数配置场景
- [x] 灵敏度范围选择 (默认0.10-0.90 游戏内灵敏度，如 0.35；eDPI = 灵敏度 × DPI)
- [x] 测试轮数设置
- [x] 目标类型选择（静止/移动）
- [x] 目标大小选择（小/中/大）
- [x] 测试模式选择
  - [x] 标准模式
  - [x] 压力模式（多目标）
  - [x] 追踪模式（移动目标）


### 2.3 测试射击场景
- [x] 创建占位场景（Phase 3 实现射击功能）

### 2.4 测试结果场景
- [x] 创建占位场景（Phase 5 实现结果展示）

---

## Phase 3: 核心测试场景

### 3.1 数据收集系统
- [x] 实现精确时间记录 (`Time.get_ticks_msec()`)
- [x] 记录以下数据：
  - [x] 命中率 (Hits/Shots)
  - [x] 首次命中时间 (从目标出现到第一次命中)
  - [x] 二次修正时间 (第一次未命中到命中的时间)
  - [x] 越过目标次数 (准星划过目标未射击)
  - [x] 总射击次数
  - [x] 目标出现时间戳
  - [x] 每次射击的时间戳
- [x] 实现每轮测试数据保存（`TestConfig.round_results`，含 sens/命中数/越靶/时间）
- [x] 实现测试进度显示（顶部 HUD：轮次/灵敏度/计时/命中数）

### 3.2 3D场景搭建
- [x] 创建测试场景 (3D)
- [x] 添加射击场背景（地面/后墙/侧墙/环境光）
- [x] 配置光照（平行光 + 环境光）
- [x] 设置摄像机（第一人称视角，FOV 由设置水平 FOV 换算）
- [x] 实现鼠标视角控制（按 cm/360° 灵敏度映射）
- [x] 实现鼠标灵敏度映射（游戏内灵敏度：VALORANT yaw = 0.07°/count，每 count 旋转 = 0.07 × 灵敏度 度，与 DPI 无关）

### 3.3 目标系统
- [x] 创建目标预制体 (Target)
  - [x] 目标模型（程序生成同心圆靶子纹理，billboard 朝向相机）
  - [x] 命中检测区域（Area3D + SphereShape，射线检测）
  - [x] 命中/未命中反馈（命中红闪 + 准星红闪）
- [x] 实现目标生成逻辑
  - [x] 随机位置生成（屏幕不同区域，防重叠）
  - [x] 不同距离生成（固定 8m 视距）
  - [x] 目标大小变化（小 0.14m / 中 0.28m / 大 0.56m ≈ 2°/4°/8° 视角）
- [x] 实现目标移动逻辑（追踪测试）
  - [x] 横向移动（边界反弹）
  - [x] 随机移动路径（左右随机方向）
- [x] 实现多目标系统（压力测试）
  - [x] 同时出现多个目标（每批 3 个）
  - [x] 目标切换逻辑（全部命中/超时后出下一批）


### 3.4 热身系统
- [x] 创建热身模式
- [x] 实现5-10个练习靶（5 个，黄色提示）
- [x] 热身数据不记录
- [x] 热身完成后自动进入正式测试


### 3.5 射击系统
- [x] 实现鼠标射击（左键点击）
- [x] 实现射线检测（命中判断）
- [x] 实现准星显示（自绘十字 + 命中红闪）
- [x] 添加射击反馈
  - [x] 命中特效（目标红闪 + 准星红闪）
  - [x] 未命中特效（未命中音效）
  - [x] 音效反馈（程序合成：射击/命中/未命中）
- [x] 实现射击冷却/延迟（时间戳完整记录，射击节奏由玩家控制）



### 3.6 测试控制
- [x] ESC键暂停功能
  - [x] 暂停菜单显示
  - [x] 继续按钮
  - [x] 退出确认按钮
- [x] Enter键退出确认（暂停菜单按钮操作）
- [x] 测试计时器（每轮耗时显示）
- [x] 当前轮次/总轮次显示
- [x] 实时统计数据展示（命中数/射击数）

> 注：轮次间灵敏度推进当前为等分采样（`TestConfig.get_round_sens()`），Phase 4 贝叶斯优化器将替换该实现；每轮目标数为 8（`TARGETS_PER_ROUND`）。

---

## Phase 4: 贝叶斯优化算法

### 4.1 基础优化器
- [x] 实现贝叶斯优化基础框架（`scripts/bayes_opt.gd`：GP 回归 + EI/UCB 采集，n≤30 高斯消元求逆）
- [x] 实现高斯过程回归
  - [x] RBF核函数（长度尺度自适应范围跨度）
  - [x] 白噪声核（σ_n = 0.12 固定，吸收单轮测量噪声）
- [x] 实现采集函数
  - [x] Expected Improvement (EI，探索项 ξ=0.01)
  - [x] Upper Confidence Bound (UCB，κ=2.0)
- [x] 实现多维目标函数（`scripts/test_objective.gd`）
  - [x] 准确率权重 (50%)：hits/targets_done（以靶为单位，超时靶=未命中）
  - [x] 响应时间权重 (30%)：1/(1 + median(t×sens))，t×sens 归一化剥离灵敏度物理优势（等效时间，衡量瞄准效率）
  - [x] 一致性权重 (20%)：1/(1 + MAD归一化 + 0.25×越靶率)，MAD 抗离群、越靶反映微调失控
  - [~] 舒适度权重 (10%)：**已删除**（评估后无可靠数据源，10% 并入准确率，重新设计为 50/30/20）

### 4.2 自适应测试策略
- [x] 实现测试阶段管理
  - [x] 探索阶段 (Exploration)：BO 阶段前 40% 用 UCB(κ=2.0)
  - [x] 利用阶段 (Exploitation)：之后用 EI
- [~] 实现动态难度调整
  - [ ] 根据准确率调整目标大小 → **已延后至 Phase 6**（与灵敏度优化是混杂变量，无法归因）
  - [ ] 根据响应时间调整目标速度 → **已延后至 Phase 6**
- [x] 实现智能灵敏度推荐
  - [x] 基于历史数据推荐下一个测试点（0.01 步长候选网格 + EI/UCB）
  - [x] UCB策略选择
  - [x] 候选排除已测点 ±0.03 邻域，避免连续重复测试同一灵敏度

### 4.3 多模态优化
- [~] 实现并行模式优化 → **已简化为结果页灵敏度倾向标签**（三并行优化器在有限轮数内统计意义低，且 cm/360° 范围定义已废弃；Phase 4.5 标签软化：不再断言手腕/手臂发力方式，仅描述"本测试中表现偏向低/中/高灵敏度"+微调建议）
  - [x] 结果页输出：低敏 <0.30 / 中敏 0.30-0.60 / 高敏 >0.60 倾向标签
- [x] 实现模式选择算法（按 GP 最优灵敏度所在区间）
- [x] 实现模式切换建议（`TestConfig.opt_summary.mode_label`）

### 4.4 冷启动处理
- [x] 实现预设灵敏度列表
  - [x] 粗扫 4 点：范围 5 等分内点，随机顺序（消除练习/疲劳轮序混杂）
- [x] 实现粗粒度→细粒度测试策略
  - [x] 粗扫 4 点 → BO 细化（默认 10 轮：粗扫 4 + 优化 6）
  - [x] 轮数 < 4 时退化为等分采样
- [x] 默认轮数：PSA 10 轮 / Consistency 3-5 轮（配置面板提示）

> 结果输出：`TestConfig.opt_summary`（推荐灵敏度/95% 置信区间/模式标签/测试 DPI 与 eDPI/样本数），test_result 基础展示，完整可视化属 Phase 5。

---

## Phase 5: 结果页面与可视化

### 5.1 结果展示
- [x] 创建结果页面UI（重构 test_result.tscn：Hero 推荐区 + 6 格指标网格 + 历史入口）
- [x] 显示推荐灵敏度（大号突出显示）
- [x] 显示性能指标（全轮聚合，`scripts/test_metrics.gd` 静态函数）
  - [x] 命中率 (%)（含超时=未命中）
  - [x] 平均首次命中时间（中位命中耗时）
  - [x] 平均二次修正时间（中位修正耗时，修正时间数组化 P1-3）
  - [x] 越过目标次数（多余开火）
  - [x] 综合评分
- [x] 显示置信区间/不确定性（得分 95% CI，clamp [0,1]）

### 5.2 数据可视化
- [ ] 实现性能分析图
  - [ ] 灵敏度-准确率折线图
  - [ ] 灵敏度-响应时间折线图
  - [ ] 灵敏度-综合评分曲线
- [ ] 实现热力图
  - [ ] 灵敏度 vs 目标速度 vs 准确率
- [ ] 实现学习曲线
  - [ ] 随测试轮次性能变化
- [ ] 实现对比表
  - [ ] 不同灵敏度下的各项指标对比

### 5.3 智能建议系统
- [ ] 实现问题识别
  - [ ] 过度瞄准检测
  - [ ] 反应过慢检测
  - [ ] 微调能力评估
- [ ] 生成个性化建议
  - [ ] 灵敏度调整方向
  - [ ] 训练建议
  - [ ] 姿势/握持建议
- [ ] 生成分享卡片
  - [ ] 灵敏度设置
  - [ ] 性能数据
  - [ ] 推荐理由

### 5.4 历史记录
- [x] 实现测试历史保存（`scripts/history_store.gd`：JSON 存储 user://history.json，含完整每轮数据，上限 100 条）
  - [x] JSON格式存储
  - [x] 包含完整测试数据
- [x] 实现历史记录查看（重构 history.tscn：列表行 = 日期/类型/灵敏度/得分/轮数 + 详情/删除按钮）
  - [x] 按日期排序（新记录在前）
  - [x] 筛选功能（全部 / PSA / Consistency）
- [~] 实现历史数据对比
  - [x] 详情弹窗：指标总览 + 每轮明细表（轮次/灵敏度/命中率/命中耗时/修正/多余开火）
  - [ ] 不同时间的灵敏度变化趋势（随 5.2 曲线图实现）
  - [ ] 性能趋势分析
- [x] 实现数据导出功能（导出 JSON 至剪贴板 / 清空记录）
  - [x] 结果页自动保存历史（含保存成功提示）

---

## Phase 6: 高级功能

### 6.1 高级测试模式
- [ ] 追踪测试完整实现
  - [ ] 目标平滑移动
  - [ ] 不同移动速度
  - [ ] 跟枪精度评估
- [ ] Flick测试实现
  - [ ] 目标出现在屏幕边缘
  - [ ] 快速瞄准评估
  - [ ] 角度变化记录
- [ ] 压力测试实现
  - [ ] 3-5个目标同时出现
  - [ ] 目标切换效率评估
  - [ ] 多目标命中率

---

## Phase 7: 优化与打磨

### 7.1 性能优化
- [ ] 对象池实现（目标复用）
- [ ] 资源异步加载
- [ ] 渲染优化（LOD、剔除）
- [ ] 算法性能优化
  - [ ] 高斯过程计算优化
  - [ ] 数据缓存机制
- [ ] 内存使用优化

### 7.2 UI/UX优化
- [ ] 动画流畅度提升
- [ ] 响应式UI（不同分辨率）

### 7.3 Bug修复与测试
- [ ] 单元测试
  - [ ] 贝叶斯优化算法测试
  - [ ] 数据收集准确性测试
- [ ] 集成测试
  - [ ] 完整测试流程测试
  - [ ] 场景切换测试


---

## Phase 8: 发布准备

### 8.1 打包
- [ ] Windows版本打包

---

## 附录：Phase 1-2 实现评估与优化建议（2026-07-31 评审）

### 总体评价

Phase 1-2 已交付可用框架：8 个场景完整串联、参数通过 `TestConfig` autoload 传递、设置持久化。代码量小、结构清晰，适合在 Phase 3 前做一轮低成本优化，避免后续重构代价累积。

### 问题清单（按优先级）

#### P1 - 功能正确性

- [x] **灵敏度范围无边界校验**（`scripts/test_config.gd`）：`sens_min` 可大于 `sens_max`，开始测试前应校验并弹窗提示（或自动交换/跟随联动）→ 已实现：开始测试时校验，无效则显示 ErrorLabel 并阻止进入，修改数值自动隐藏提示；Consistency 单点模式跳过范围校验（不再误报）
- [x] **Consistency 测试与灵敏度范围逻辑冲突**（`test_select.gd` / `test_config.gd`）：Consistency 是固定灵敏度测试，仍显示 20-80 范围输入框，语义不符。应联动：选择 Consistency 时隐藏/禁用范围控件，改为单个灵敏度输入 → 已实现：Consistency 时隐藏最大灵敏度行，最小灵敏度行改为"灵敏度 (cm/360°)"，保存时 max=min
- [x] **测试流程状态未重置**：从测试中途返回主菜单再进入，`TestConfig` 保留旧值 → 已实现：主菜单进入测试流程时调用 `TestConfig.reset()`（上一步返回不重置）
- [x] **`change_scene_to_file` 无错误处理**（所有场景脚本）→ 已实现：新增 `scripts/scene_nav.gd` 封装，检查返回值并输出错误

#### P2 - 视觉与体验

- [x] **AcceptDialog 弹窗为默认系统样式**（`test_select.tscn`）→ 已实现：自定义 Popup 面板（遮罩 + 居中卡片 + 说明文本 + 知道了按钮），与主题一致
- [x] **OptionButton 下拉菜单为默认浅色样式** → 已实现：主题补全 `PopupMenu` 深色面板/hover 红底/加速键红色
- [x] **开始测试按钮 hover 无反馈**（`test_config.tscn`）→ 已实现：normal 红边框深底、hover 红底、pressed 深红 三态
- [x] **toggle 选中态视觉区分弱**（`test_select.tscn`）→ 已实现：主题新增 `Button/styles/toggle_pressed`，选中态深红底 + 左侧 6px 红色高亮条
- [x] **无 ESC 快捷操作** → 已实现：`scripts/esc_nav.gd` 通用 ESC 导航节点（test_shoot/test_result 使用），test_select ESC 先关弹窗再回主菜单，test_config ESC 回上一步
- [x] **UI 缩放缺失** → 已实现：分辨率自适应。基准 1280x720，`fractional` 缩放模式，自动模式按窗口尺寸计算 `content_scale_factor`（下限 0.5），设置中提供 自动/100%/125%/150% 选项，保存至 settings.cfg `display/ui_scale`

#### P3 - 架构与扩展性

- [x] **占位场景结构重复**（`test_shoot` / `test_result` / `history` / `about`）→ 已实现：抽出共享子场景 `ui/top_bar.tscn`（标题栏 + 分隔线，`@export title_text`），7 个场景已替换
- [x] **`TestConfig` 数据模型需前瞻扩展**（`scripts/test_session.gd`）→ 已实现：新增 `current_round` / `current_sens` / `round_results`（Phase 3 数据容器）与 `reset()`
- [x] **设置 DPI/FOV 尚未接入测试流程** → 已实现：`TestConfig` 启动时加载设置并暴露 `get_dpi()` / `get_fov()`，Phase 3 灵敏度映射可直接使用
- [ ] **场景切换无过渡**：延后至 Phase 7 打磨项
- [~] **主题覆盖不完整**（`ui/theme_valorant.tres`）：已补全 PopupMenu / HSlider / toggle 样式；SpinBox 箭头按钮与 CheckBox 仍为默认样式（项目暂未使用 CheckBox，箭头图标需自制，优先级低）
- [ ] **窗口元数据缺失**：图标与窗口标题延后至发布准备处理

### 建议执行时机

- **Phase 3 开工前**：P1 全部 + P3 的 TestConfig 数据模型扩展（射击场景会重度依赖）
- **Phase 5 前后**：P2 弹窗/下拉样式（结果页会有更多交互组件）
- **Phase 7 打磨**：过渡动画、主题补全、UI 缩放


---

## 附录：Phase 4 实现评估与设计决策（2026-07-31 评审）

### 总体评价

Phase 4 已交付：GP 回归（RBF + 白噪声）+ EI/UCB 采集 + 多维目标函数 + 冷启动粗扫策略，headless 单测 17 项全部通过（含合成数据收敛验证：10 轮内推荐收敛到已知峰值 0.45 ±0.08）。测试流程已全面接管：PSA 模式由 `test_plan.gd` 选点，Consistency 模式直通固定单点。

### 信度评估（目标函数可重复性）

**核心结论：原设计每轮 8 靶噪声 SD ≈ 0.083，BO 细化阶段相邻灵敏度真实差异 < 0.05，噪声 > 信号。**

| 维度 | 单轮噪声 | 缓解措施（已实施） |
|---|---|---|
| accuracy（50%） | 命中率 SD ≈ 0.16 | 每轮靶数 8 → 12（`TARGETS_PER_ROUND`） |
| speed（30%） | 0.023 | 均值 → 中位数（抗离群） |
| consistency（20%） | 0.006 | std → MAD（抗离群） |
| 轮序混杂 | 练习/疲劳偏向后半段 | 粗扫 4 点随机顺序（原单调推进已废弃） |
| 过拟合 | — | GP 白噪声项 σ_n = 0.12 固定 |

### 效度评估

- **构念覆盖**：准确 + 快速 + 稳定三维度合理；**越靶次数（overshoots）纳入一致性维度**（0.25 惩罚系数），补上"微调失控"这一核心构念；correction_time 保留用于结果页细分展示
- **外部效度**：目标固定 4° 视角只覆盖一种难度；结果页标注测试 DPI 与 eDPI，避免换算歧义
- **诚实呈现不确定性**：结果页展示 95% 置信区间，不以单点断言

### 设计决策记录

1. **舒适度权重删除**（用户决策）：无可靠数据源，10% 并入准确率 → 50/30/20
2. **动态难度调整移入 Phase 6**（用户决策）：与灵敏度优化是混杂变量，无法归因
3. **多模态简化**（用户决策）：三并行优化器 → 最优灵敏度区间映射"手臂流/混合/手腕流"标签
4. **数据补全**：`target.gd` 记录 `spawn_ms`，`round_data.hit_times` = 每靶出现→命中耗时（响应时间与一致性的共同数据源）
5. **轮数预算**：PSA 默认 10 轮（粗扫 4 + BO 6），Consistency 3-5 轮；轮数 < 4 退化等分采样
6. **BO 选点防重复**：候选排除已测点 ±0.03 邻域（EI 收敛阶段会反复建议最优点，真实测试连续重复体验差）

### 已验证

- headless 单测 17 项：GP 数值（erf/CDF 精度）、目标函数边界（全中/全空/离群/越靶）、Consistency 回归、BO 收敛性
- `--headless --quit` 启动无脚本错误
- 待手测：完整 10 轮 PSA 流程 + 结果页展示（Phase 5 可视化前的验收）


---

## 附录 B：Phase 4.5 目标函数与任务设计修订（2026-07-31 评审）

### 背景（用户实测反馈）

1. 靶心太大（中靶 4° 视角）命中率普遍虚高 → 天花板效应，准确率维度失去区分度
2. 灵敏度越高到达时间越短、越低越长 → **系统性物理偏差**（到达时间 t ∝ θ/sens，绝对时间进目标函数结构性偏袒高敏）
3. 6s 超时无时间压力 → 命中率/时间双重失真
4. 手臂发力却被推荐手腕流 → 物理偏差 + 短距任务构念偏差（4° 靶腕部微调足够，手臂大范围拉枪能力未被测量）+ 标签绝对区间假设粗糙

### 修订内容（已实施）

| # | 修订 | 文件 |
|---|---|---|
| 1 | speed = 1/(1 + median(t×sens))，剥离灵敏度物理优势（恒速模型下 t×sens 与 sens 无关） | test_objective.gd |
| 2 | acc = hits/targets_done（以靶为单位，超时靶=未命中） | test_objective.gd |
| 3 | 目标存活 6s → 3s，恢复时间压力 | test_session.gd |
| 4 | 靶子缩小一档：小 0.12m(1.7°) / 中 0.20m(2.9°) / 大 0.40m(5.7°) | test_shoot.gd |
| 5 | 大角度拉枪靶：65% 短距（角距 8°-18°）+ 35% 长距（水平 28°-46°，垂直 FOV 有限故只做水平大角度），tan 投影到 z=-8 平面保持 8m 视距恒定，y clamp ±2.0 | test_shoot.gd |
| 6 | 模式标签软化：仅描述"表现偏向低/中/高灵敏度"+微调建议，不断言发力方式 | test_shoot.gd |

### 验证

- 偏差剥离：sens=0.8/t=0.5s 与 sens=0.2/t=2.0s（t×sens 相同）speed 分完全相同 ✓
- acc 分母：12 靶 10 命中（2 超时）→ 0.833 ✓
- 角距分布（100 次采样）：长距 31%（26°-45°）、短距 69%、最大 45.3° ✓
- 回归：全中/全空/离群/越靶用例全部保持 ✓
- 待手测：3s 超时节奏、大角度靶难度、结果推荐合理性


---

## 附录 C：Phase 4.5 实施后复审评估与优化空间（2026-07-31 复审）

> 复审方式：代码走查（test_shoot.gd / test_plan.gd / test_objective.gd / bayes_opt.gd / target.gd / test_session.gd / test_result.gd）。
> 复审发现了 **4 项 P0 效度问题**——均为此前 headless 单测未覆盖的真实游玩路径（单测只覆盖了相机朝向为默认值的情况）。

### P0 - 效度问题（已全部修复，commit 待补）

#### P0-1 靶位采样未使用相机朝向，Phase 4.5 角距设计在真实游玩中失效 ✅

**现象**：`_spawn_position()` 只读取 `camera.global_position`，yaw_off/pitch_off 是**世界坐标角度**而非相对相机朝向。单测通过是因为相机 yaw=0（默认朝向）。

**根因**：打完上一个靶后玩家相机朝向任意（yaw ∈ -180°~180°）。靶子按世界角度生成 → 屏幕角距完全随机：设计的"短距 8°-18° + 长距 28°-46°"分布被破坏，甚至可能生成在屏幕外（玩家朝左时，世界 +8° 的"短距"靶实际在屏幕 30°+ 处）。

**修复方案（已实施）**：相对方向 `rel`（tan 投影）按相机完整朝向旋转 `Basis(UP, _yaw) * Basis(RIGHT, _pitch)`（YXZ，与 `_apply_mouse_motion` 一致）。**注意：最初只绕 Y 旋转导致相机 pitch 时角距仍偏移（测试暴露），需同时应用 pitch。**

#### P0-2 世界坐标 y clamp 与相机 pitch 冲突 ✅

**现象**：`pos.y = clampf(pos.y, -2.0, 2.0)` 是世界坐标限制。玩家抬头（pitch=30°）时屏幕中心在 y=1.6+tan(30°)×8=6.2m，靶子被压到 y=2.0 → 屏幕角距变成 -27°（屏幕外/错误位置）。

**修复方案（已实施）**：删除世界 y clamp。相对 pitch 采样已限制在 ±18°（远小于垂直半视场 33°@103 FOV），几何上无需额外限制。

#### P0-3 speed 未按角距归一化，大角度靶结构性惩罚 ✅

**现象**：hit_times 记录的是"出现→命中"绝对时间，无每靶角距。46° 靶 0.8s 命中 vs 8° 靶 0.3s 命中 → t×sens 分别为 0.28 / 0.105 → speed 分 0.78 / 0.90。**同一位玩家、同一灵敏度**，只因为该轮随机到大角度靶多，speed 分就系统性偏低 → 优化器把"运气差的一轮"当成"灵敏度差"。

**根因**：Phase 4.5 引入大角度靶后，`t×sens` 归一化只剥离了灵敏度变量，未剥离角距变量（原设计所有靶同为 4° 视角，无此问题）。

**修复方案（已实施）**：
1. `target.gd` 新增 `angle_rad`（setup 传入，spawn 时相对相机初始朝向的角距）
2. `test_shoot.gd` 命中时记录 `hit_angles`（与 hit_times 平行数组）
3. `speed = 1/(1 + median(t×sens/θ))`——t×sens/θ 物理意义 = 鼠标物理移动距离/目标角距 = **轨迹效率**（无量纲，>1 说明有修正）；无角距数据时退化为 t×sens（θ=1）

#### P0-4 低灵敏度 + 3s 超时 + 46° 长距 = 低敏系统性惩罚 ✅

**现象**：46° 长距靶在 sens=0.10 时需 46.2°/(0.07×0.10)=6600 counts 鼠标移动（≈800DPI 下 21cm），峰值手速 2500-3000 count/s 需 2.3-2.7s 纯移动，加精瞄必然超 3s → 低敏长距靶超时率 50%+。**而 0.15-0.30 正是手臂流应推荐的区间**——Phase 4.5 想让手臂流被看见，这个机制反而把该区间打低。

**修复方案（已实施）**：长距角度区间与 `sens_min` 联动（等效 counts 工作量一致）：
```gdscript
var t_low := clampf((TestConfig.sens_min - 0.10) / 0.30, 0.0, 1.0)
var long_lo := lerpf(20.0, 28.0, t_low)
var long_hi := lerpf(22.0, 46.0, t_low)
```
（sens_min=0.10 → 长距 20°-22°，3s 内可达 ✓；0.40 → 28°-46°）

#### P0-5 overshoot（越靶）判定产生伪信号 ✅

**现象**：`_update_overshoot_detection()` 每帧射线判定"准星是否曾瞄准过靶子"，命中判定只要 `was_aimed` 从 false→true 即 +1。**任何正常瞄准路径都必然先扫过靶心再停下开火**（除非像素级直接落靶）→ overshoots ≈ 每靶至少 1 次，一致性维度（20% 权重 × 0.25 惩罚）被系统性污染，且与灵敏度无关（无法区分灵敏度差异）。

**修复方案（已实施）**：改为"命中前多余开火数"（`Σ(target.shots_against - 1)`，`_on_target_hit` 时累加），语义为打空枪次数，精确无伪信号，删除 `_update_overshoot_detection` 每帧射线逻辑（顺带省性能）。

### P1 - 重要优化（建议 Phase 5 前完成）

#### P1-1 GP signal_variance 未按数据尺度归一化 ✅

`signal_variance=1.0` 固定，但 score 实测方差仅 ~0.03-0.05 → GP 置信区间被高估 4-5 倍 → EI/UCB 过度探索、结果页 95% CI 过宽（score_low 可 <0）。修复：`_train()` 时 `signal_variance = maxf(y 样本方差, 0.001)` 动态设置；`score_low/high` clamp 到 [0,1]。✅ 已实施（bayes_opt.gd / test_shoot.gd），单测验证方差自适应。

#### P1-2 移动靶 + 长距出界 ✅

sens_min=0.10 时（P0-4 修复后）长距 22° 在 x=3.2m 仍在 bounds ±4.5 内；但高敏长距 46° → x=8.3m 出生即出界，被 clamp 到 4.5 反弹 → 角距实际 29°。修复：**已实施**——移动靶水平偏移 clamp ±15°（tan15°×8=2.14m + 0.8m/s×3s=2.4m 位移 ≈ 4.5m 边界临界，留余量）；移动靶不再出长距（等效移动任务单独评估）。TRACKING 模式 1.8m/s 位移 5.4m 仍会出界，属 Phase 6 追踪模式重做范围。

#### P1-3 correction_time 只保留最后一靶 ✅

`round_data["correction_time"]` 每次命中覆盖 → 只剩最后一靶的数据。修复：**已实施**——改为 `correction_times` 数组（每靶首次开火→命中耗时），结果页/历史详情展示中位修正耗时。

#### P1-4 指针加速未提示

Godot 无 raw input，`InputEventMouseMotion.relative` 受 Windows"提高指针精确度"影响 → 灵敏度映射失真（换算基准 800DPI 也依赖系统设置）。设置页/测试说明页应提示关闭指针加速。→ 待办：随 Phase 5.3 建议系统/说明页一起做（延后）。

### P2 - 延后项（Phase 5/6/7 已计划）

- 灵敏度轴的 95% CI（当前只显示得分 CI）→ Phase 5.2 曲线图阶段
- 追踪模式独立实现（跟枪精度、连续命中数据）→ Phase 6
- 对象池、场景过渡动画 → Phase 7

### 验证计划（修复后）

- 新增单测（14 项全过，verify_p5.gd 已执行后删除）：历史存储增删清空回读、指标聚合（命中率/中位耗时/修正/多余开火/轮均分）、GP 方差自适应（低方差数据收缩、高方差数据保持）、移动靶水平偏移 ≤15°
- 回归：既有单测保持通过（speed 退化路径兼容）
- 待手测：完整跑一次测试 → 结果页指标展示 → 历史记录列表/详情/筛选/导出

### 建议执行时机

P0 五项已修复（提交 `待补`）；P1 随 Phase 5 结果页一起做（结果页会依赖 CI 与 correction_times）；P2 按原计划排期。


---




